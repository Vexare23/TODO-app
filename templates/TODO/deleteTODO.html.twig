{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(TODOForm, {
        attr: { class: 'form1 '}
    }) }}
    <h1>Delete current TODO</h1>

    {{ form_row(TODOForm.name) }}

    {% if is_granted('ROLE_ADMIN') %}
        {{ form_row(TODOForm.status) }}
    {% endif %}

    <button type="submit" class="btn btn-question">Delete</button>

    {{ form_end(TODOForm) }}

{% endblock %}
